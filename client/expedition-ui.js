import {DOCTRINES} from '/shared/expedition-doctrines.js';
export function expeditionUI(data,disabled=''){
 const r=data.player,current=r.expedition?.doctrine||r.doctrine||'standard',wins=r.adventure.doctrineWins||{};
 return `<section class="legacy-board expedition-council"><header><small>PREPARE A PRÓXIMA AVENTURA</small><h3>Conselho de expedição</h3><p>Escolha a regra de abertura. Ela vale para você e seu adversário em cada mesa; dungeons mantêm a escolha até o fim. Sem custo extra ou bônus na arena online.</p></header><div class="legacy-grid">${Object.entries(DOCTRINES).map(([id,d])=>`<article class="${current===id?'doctrine-selected':''}"><img src="/assets/world/${d.board}.png" alt=""><div><small>NÍVEL ${d.level} · ${wins[id]||0} EXPEDIÇÕES CONCLUÍDAS</small><h4>${d.icon} ${d.name}</h4><p>${d.text}</p><button data-doctrine="${id}" aria-pressed="${current===id}" ${disabled||r.expedition||r.level<d.level||current===id?'disabled':''}>${current===id?'PREPARAÇÃO ATIVA':r.level<d.level?'DESBLOQUEIA NO NÍVEL '+d.level:'PREPARAR'}</button></div></article>`).join('')}</div><p>Vença a última mesa e confira histórias e conquistas. Vitórias repetidas na mesma mesa respeitam o intervalo de recompensa.</p></section>`;
}
